import { TimerIcon } from "lucide-react";

export default function DashboardPage() {
    return (
        <div className="w-full h-48 bg-white rounded-xl flex flex-col items-center justify-center gap-2">
            <TimerIcon size={48} />
            <div>Coming Soon...</div>
        </div>
    );
}
